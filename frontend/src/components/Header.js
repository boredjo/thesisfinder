 // /frontend/src/components/Header.js

import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>ThesisFinder</h1>
      <nav>
        <a href="#">Log in</a>
        <a href="#">Join for free</a>
      </nav>
    </header>
  );
};

export default Header;
